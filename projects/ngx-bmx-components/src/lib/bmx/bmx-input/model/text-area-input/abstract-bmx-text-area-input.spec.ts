import {AbstractBmxTextAreaInput} from './abstract-bmx-text-area-input';

describe('AbstractBmxTextAreaInput', () => {
  it('should create an instance', () => {
    expect(new AbstractBmxTextAreaInput()).toBeTruthy();
  });
});
