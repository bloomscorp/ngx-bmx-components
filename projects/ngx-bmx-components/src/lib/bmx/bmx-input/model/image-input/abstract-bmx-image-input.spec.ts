import {AbstractBmxImageInput} from './abstract-bmx-image-input';

describe('AbstractBmxImageInput', () => {
  it('should create an instance', () => {
    expect(new AbstractBmxImageInput()).toBeTruthy();
  });
});
