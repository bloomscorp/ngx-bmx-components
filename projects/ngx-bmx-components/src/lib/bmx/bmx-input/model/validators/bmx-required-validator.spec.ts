import {BmxRequiredValidator} from './bmx-required-validator';

describe('BmxRequiredValidator', () => {
  it('should create an instance', () => {
    expect(new BmxRequiredValidator()).toBeTruthy();
  });
});
