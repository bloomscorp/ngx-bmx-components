import {BmxMaxLengthValidator} from './bmx-max-length-validator';

describe('BmxMaxLengthValidator', () => {
  it('should create an instance', () => {
    expect(new BmxMaxLengthValidator()).toBeTruthy();
  });
});
