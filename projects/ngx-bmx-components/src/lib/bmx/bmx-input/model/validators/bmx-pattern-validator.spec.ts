import {BmxPatternValidator} from './bmx-pattern-validator';

describe('BmxPatternValidator', () => {
  it('should create an instance', () => {
    expect(new BmxPatternValidator()).toBeTruthy();
  });
});
