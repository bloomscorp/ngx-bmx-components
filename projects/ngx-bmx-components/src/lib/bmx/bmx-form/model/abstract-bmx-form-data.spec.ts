import { AbstractBmxFormData } from './abstract-bmx-form-data';

describe('AbstractBmxFormData', () => {
  it('should create an instance', () => {
    expect(new AbstractBmxFormData()).toBeTruthy();
  });
});
