import {BmxDefaultEmailValidator} from './bmx-default-email-validator';

describe('BmxDefaultEmailVlidator', () => {
  it('should create an instance', () => {
    expect(new BmxDefaultEmailValidator()).toBeTruthy();
  });
});
