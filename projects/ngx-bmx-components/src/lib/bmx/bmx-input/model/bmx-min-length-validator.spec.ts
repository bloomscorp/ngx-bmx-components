import { BmxMinLengthValidator } from './bmx-min-length-validator';

describe('BmxMinLengthValidator', () => {
  it('should create an instance', () => {
    expect(new BmxMinLengthValidator()).toBeTruthy();
  });
});
